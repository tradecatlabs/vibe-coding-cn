Vibe Coding，从六根箭头开始

一段代码，四百行。

一个需求，十七个字。

从十七个字走到四百行，中间发生了什么。

以前我把这件事叫编程。

后来我觉得不够准确。

因为真正麻烦的不是四百行代码。

是那十七个字。

它们要先变成另一种东西。

目标。
约束。
文件。
函数。
命令。
测试。
运行结果。
失败日志。

最后再变回一句话：

可以用了。

这中间每发生一次变化，都有一根箭头。

范畴论把它叫态射。

我后来发现，Vibe Coding 很大一部分事情，都可以从这里开始讲。

对象。

箭头。

箭头接箭头。

然后检查，我们到底有没有走到原来要去的地方。

先说六件东西

第一次看范畴论的时候，我以为它会很难。

后来发现，我们这里只需要六个词。

对象。

态射。

复合。

恒等态射。

交换图。

对偶。

名字都比事情本身吓人。

一，对象

假设你对 AI 说：

帮我给这个项目加一个登录功能。

这句话是一个对象。

AI 理解出来的需求，是另一个对象。

修改后的代码，又是一个对象。

测试结果还是对象。

运行起来以后数据库里的状态，也是对象。

甚至最后那句：

“测试通过了。”

也是对象。

对象不神秘。

它只是回答一个问题：

现在桌子上放着什么。

可以是一段输入。

一份计划。

一个仓库。

一段代码。

一个 JSON。

一个数据库状态。

一张测试报告。

一条错误日志。

或者一个人脑子里的目标。

只要我们暂时把它当成一个整体，它就可以是对象。

二，态射

对象本身不会做事。

变化才会。

需求变成计划。

计划变成代码。

代码被运行。

运行结果被检查。

这些变化，就是态射。

写成最简单的样子：

```text
输入 ──变化──→ 输出
```

比如：

```text
需求 ──理解──→ 计划
计划 ──生成──→ 代码
代码 ──执行──→ 系统状态
系统状态 ──测试──→ 证据
```

我后来很喜欢这样看 Agent。

不是因为它更数学。

是因为很多模糊的事情，一旦画成箭头，就没地方躲了。

“AI 帮我改一下。”

改什么。

从什么变成什么。

谁来改。

改完以后拿什么证明。

箭头一画，问题就出来了。

态射只问三件事。

从哪里来。

到哪里去。

中间到底做了什么。

三，复合

有一天，一个 Agent 连续做了七件事。

读需求。

找文件。

改代码。

装依赖。

跑测试。

修错误。

重新跑测试。

最后它告诉我：

完成了。

从外面看，这是一件事。

从里面看，是七根箭头。

范畴论说，只要前一根箭头的终点，正好是后一根箭头的起点，它们就可以接起来。

如果：

```text
f: A → B
g: B → C
```

那么：

```text
g ∘ f: A → C
```

Vibe Coding 也是一样。

```text
输入
→ 理解
→ 计划
→ 修改
→ 执行
→ 验证
→ 结果
```

这叫复合。

Agent 看起来像一个东西。

其实里面经常是一长串复合。

模型生成一次。

工具执行一次。

环境返回一次。

模型再判断一次。

再执行。

再返回。

如此循环。

最后用户只看到一句：

Done.

复合还有一个条件。

结合律。

听起来像教材里的东西。

工程里却很现实。

你把前面几步先组合，还是把后面几步先组合，最后规定的结果不能偷偷变掉。

否则那不是工作流。

那是漂移。

四，恒等态射

有一种箭头很奇怪。

它从 A 出发。

最后还是 A。

```text
id_A: A → A
```

什么都没有改变。

这叫恒等态射。

一开始我觉得这东西没用。

后来发现软件工程里，到处都需要它。

用户给了一个日期。

Agent 转手传给工具。

日期不能顺手改了。

schema 里写的是字符串。

经过三层调用以后，它还应该是那个字符串。

用户说：

不要改 API。

Agent 重构一圈回来，API 还得是原来的 API。

这就是恒等。

有些步骤的职责，不是创造。

是保留。

范畴论写：

```text
id_B ∘ f = f = f ∘ id_A
```

工程师可以把它翻译成人话：

别在传东西的时候顺手把东西改了。

很多 Agent 的事故，就发生在这里。

它看起来只是在搬运上下文。

其实已经重写了上下文。

它看起来只是在总结要求。

其实已经删掉了一个限制。

然后后面的每一步都很正确。

只是正确地完成了另一件事。

五，交换图

后来我越来越少问：

代码写得对不对。

我开始问另外一个问题：

这些路径，最后是不是落在同一个地方。

比如需求说：

用户退出登录以后，token 必须失效。

有两条路。

第一条：

```text
需求 → 实现 → 运行结果
```

第二条：

```text
需求 → 测试 → 验收结果
```

画出来：

```text
A ──f──→ B
│        │
h        g
│        │
↓        ↓
C ──k──→ D
```

如果：

```text
g ∘ f
```

和：

```text
k ∘ h
```

最后满足的是同一个规定关系，

图就交换。

我第一次真正意识到交换图的重要，是看见一个项目：

代码通过。

测试通过。

CI 通过。

但是功能是错的。

后来查了很久。

不是代码没实现测试。

是测试根本没有实现需求。

那张图从一开始就不交换。

代码和测试非常一致。

它们一起错了。

所以类型有用。

schema 有用。

测试有用。

CI 有用。

检查清单有用。

代码审查也有用。

它们做的是同一件事：

把“应该一致”这句话，变成机器能够检查的东西。

六，对偶

范畴论还有一个很好用的动作。

把所有箭头反过来。

原来是：

```text
原因 → 结果
```

现在看：

```text
结果 → 原因
```

严格说，不是把现实里的因果真的倒过来。

是换一个方向描述结构。

范畴论把这个叫对偶。

工程里，我经常这么用。

功能坏了。

不要马上改代码。

先从坏掉的结果往回走。

哪个测试应该发现它。

测试依赖哪个约束。

约束来自哪个需求。

需求是哪一步被理解错了。

同样，做一个新功能，也可以反着来。

最后要交付什么。

要证明它，需要什么证据。

要产生这些证据，需要什么测试。

要通过这些测试，需要什么实现。

要得到这个实现，需要改哪些对象。

这就是从验收标准往回推。

正着走是建设。

反着走是审查。

很多时候，后者更重要。

关于 Vibe Coding 的五件事

第一件，先把 AI 本身的问题解决

Demis Hassabis 有一句话：

> “首先解决人工智能问题，然后再用人工智能解决其他所有问题。”

我以前觉得这句话很宏大。

后来发现可以把它理解得很具体。

一个人的意图。

要先穿过模型。

再穿过工具。

穿过权限。

穿过运行环境。

穿过反馈循环。

最后落到现实世界。

如果这条路本身不可靠，AI 能做的事情越多，问题只会越大。

在这个结构里，人给目标。

也给限制。

什么能做。

什么不能做。

做到什么程度算完成。

模型负责理解、推理和生成候选动作。

Harness 负责另外一堆没人愿意在演示视频里讲的东西。

上下文。

工具。

权限。

调用。

状态。

重试。

错误处理。

执行循环。

最后还得有人或者机器来验收。

所以真正的路径是：

```text
人的意图
→ 可理解的目标
→ 可执行的候选行动
→ 真实执行
→ 证据
→ 判断
```

模型只是中间的一段。

很重要。

但不是全部。

第二件，模型首先生成的是符号

模型说：

我已经帮你修好了。

这句话没有任何特殊力量。

它只是又生成了一串 token。

模型可以生成 Python。

可以生成 SQL。

可以生成 shell。

可以生成 JSON。

可以生成工具调用。

可以生成一份看起来非常像已经完成的报告。

这些东西有一个共同点。

它们首先都是符号。

我把模型在当前词表、上下文和工具协议下能够直接表达出来的东西，叫生成域。

生成域越来越大。

以前模型只能写代码。

后来可以调用工具。

可以操作浏览器。

可以改仓库。

可以跑测试。

可以继续根据错误修复。

但有一件事一直没变。

生成出来，不等于发生过。

写出：

```bash
pytest
```

不等于测试通过。

写出：

```sql
DELETE FROM users;
```

也不等于数据库真的执行了。

模型说文件已经保存，

不等于磁盘上存在那个文件。

模型说部署成功，

更不等于生产环境真的健康。

所以我现在看到任何 Agent 输出，都会下意识分两栏。

左边：

它生成了什么。

右边：

什么证据证明这件事真的发生了。

中间差的那一段，就是工程。

第三件，模型会吞掉中间层

几年前，我们给模型写很长的 Prompt。

后来做 Prompt 模板。

再后来做工作流。

Agent。

Planner。

Router。

Memory。

Retriever。

各种胶水层。

每一层都很合理。

每一层也都有人专门维护。

然后模型能力涨了一截。

有几层突然没必要了。

以前需要十个 Prompt 才能稳定做的事情，后来一句话就能做。

以前需要工作流固定的顺序，后来模型自己会规划。

以前要专门做一层格式适配，后来模型可以直接遵守 schema。

这件事会一直发生。

我叫它模型吞噬。

所有中间层都可以看成态射。

```text
输入
→ Prompt
→ Router
→ Planner
→ Retriever
→ Tool Adapter
→ Executor
→ 结果
```

如果模型能够直接学会其中几段，这几根箭头就会被压缩。

有些公司会发现，自己维护两年的核心系统，下一代模型原生就会了。

这很正常。

真正不容易被吞掉的是边界。

真实权限。

真实系统。

真实数据。

物理执行。

独立验证。

谁可以删生产数据库。

谁可以批准付款。

这不是模型聪明以后就应该消失的东西。

恰恰相反。

模型越强，边界越应该清楚。

第四件，生成和审查必须分开

我以前见过一种审查。

Agent 写完代码以后问自己：

代码有没有问题？

Agent 看了一遍。

说：

没有。

这不能叫审查。

这最多叫回忆。

生成路径和验证路径最好分开。

原始目标在这里。

候选结果在另一边。

验证者重新读一次目标。

重新读限制。

重新看代码。

重新看运行结果。

重新看测试。

然后再判断。

```text
原始目标 ──生成路径──→ 候选结果
    │                    │
    └────验证路径────────→ 证据
```

重点是：

验证路径不能只听生成者讲故事。

如果写代码的 Agent 说：

“我已经保证了线程安全。”

审查者应该去看锁。

如果它说：

“测试已经覆盖边界情况。”

审查者应该去看测试。

如果它说：

“这个改动不影响 API。”

应该比较接口。

我们不需要怀疑模型。

也不需要相信模型。

需要的是证据。

这是两回事。

第五件，先拼已有能力

有一次有人让我做一个功能。

大概二十个步骤。

最初计划里准备自己写：

认证。

任务队列。

缓存。

文件存储。

日志。

重试。

监控。

权限。

后来算了一下。

真正属于业务本身的代码不到百分之十。

剩下的全是别人已经解决过很多年的问题。

我后来把这种做法叫拼好码。

先找成熟能力。

库。

平台。

仓库。

服务。

协议。

API。

然后把它们复合起来。

```text
需求
→ 找能力
→ 组合能力
→ 做适配
→ 加业务规则
→ 验证
```

只有现有能力真的满足不了关键限制时，才自己写新的核心部分。

这不是因为少写代码比较高级。

只是因为每多写一行自己拥有的核心代码，就多了一行以后必须自己证明正确、自己升级、自己修、自己背责任的东西。

成熟能力是一根已经被很多人走过的箭头。

你当然可以自己再造一根。

只是最好知道为什么。

一次完整的 Vibe Coding 到底发生了什么

我后来把整个过程画成这样：

```text
现实问题 X
  ↓
目标结构 S
  ↓
候选方案 C
  ↓
结果状态 S'
  ↓
证据 E
```

再把箭头写出来：

```text
X ──I──→ S ──M──→ C ──T──→ S' ──V──→ E
```

五个对象。

四段主要变化。

事情一下就清楚了。

`X` 是现实问题。

比如：

登录太慢。

首页打不开。

帮我写一个财务模型。

把这份报告改成英文。

给项目加权限系统。

这些都是原始世界里的问题。

它们通常很脏。

缺信息。

有歧义。

夹杂默认条件。

甚至用户自己也没有完全想明白。

所以第一根箭头叫 `I`。

识别。

描述。

形式化。

它把现实问题变成目标结构 `S`。

这里开始出现真正可以工作的东西。

目标是什么。

有哪些对象。

限制是什么。

哪些文件可以动。

什么不能变。

什么叫完成。

然后是 `M`。

Model。

模型读取 `S`。

生成候选方案 `C`。

注意，是候选。

这个词不能省。

模型可以非常聪明。

候选方案也可以看起来非常完整。

但在执行以前，它还没有碰到现实。

然后是 `T`。

这一段经常比模型本身长。

Harness。

工具。

运行时。

权限系统。

文件系统。

浏览器。

shell。

数据库。

API。

所有真正和世界发生接触的东西。

它们把候选方案变成新的系统状态 `S'`。

文件真的改了。

服务真的启动了。

接口真的返回了。

数据库里真的多了一行。

到这里，事情才发生。

最后是 `V`。

验证。

测试。

审查。

类型检查。

schema 校验。

lint。

CI。

人工验收。

监控。

截图。

日志。

diff。

它们把状态 `S'` 变成证据 `E`。

这一步告诉我们：

不仅事情发生了。

而且发生的是我们原来要求的那件事。

完整路径是：

```text
现实
→ 描述
→ 生成
→ 执行
→ 验证
```

很多失败的 Vibe Coding，只做了前三个字。

现实。

描述。

生成。

然后模型说：

完成。

用户也说：

好。

中间两根箭头没有了。

执行没有证据。

验证没有证据。

最后只有一种东西很完整。

语气。

后来我把这套东西压成八条。

不是原则墙。

是出问题的时候拿出来看的。

一，先找对象

不要一上来就让模型行动。

先问桌子上有什么。

输入。

目标。

状态。

代码。

配置。

权限。

结果。

证据。

对象没找全，后面的箭头大概率会接错。

二，每根箭头都写清楚

一个动作至少应该知道：

输入是什么。

输出是什么。

做之前要求什么。

做完以后保证什么。

失败会留下什么。

如果一句“处理一下”包含五种可能行为，那它还不是一个好态射。

三，任务不是步骤列表，是复合

步骤列表只告诉你先做什么后做什么。

复合还要多问一句：

上一步的输出，真的是下一步需要的输入吗。

很多工作流看起来有十步。

实际上第四步以后就已经没有使用第三步的结果了。

后面六步只是继续表演。

四，用交换图查一致性

需求。

实现。

测试。

文档。

配置。

交付结果。

它们不是六件互不相关的东西。

它们应该指向同一个规定结果。

需求说一套。

代码做一套。

测试测第三套。

文档写第四套。

CI 全绿。

这没有任何值得高兴的。

图根本不交换。

五，失败以后倒着查

最后结果错了。

往回走。

验证错了吗。

执行错了吗。

候选方案错了吗。

目标结构错了吗。

还是最开始，现实问题就理解错了。

从结果倒推条件。

比重新生成一遍通常便宜。

六，Agent 不是模型

模型是一个重要器官。

不是整个身体。

一个 Agent 系统里还有：

上下文。

记忆。

工具。

权限。

状态。

环境。

循环。

错误处理。

验证。

一个模型在 benchmark 上提高五个百分点，

不代表 Agent 系统就提高五个百分点。

有时候甚至没有变化。

因为真正坏掉的是另外一根箭头。

七，人负责选择

AI 很擅长扩大候选空间。

它可以更快理解。

更快规划。

更快生成。

更快试。

但是：

要什么。

不要什么。

愿意承担什么风险。

什么东西值得做。

什么东西做到哪里就该停。

这些最终还是选择。

人应该待在选择的位置。

机器应该尽可能承担搜索和执行。

门禁应该承担那些可以形式化的判断。

不要三者互相扮演。

八，最后看证据

我现在越来越不在意一句话听起来像不像完成。

我看：

测试结果。

类型检查。

schema。

日志。

截图。

diff。

CI。

检查清单。

真实运行状态。

一句话可以非常自信。

证据不会因为语气好听就变多。

Vibe Coding 最后不是：

```text
Prompt → Code
```

而是：

```text
目标
→ 候选行动
→ 真实执行
→ 独立证据
```

前面决定方向。

中间改变世界。

最后证明没有改错。

六个词

写到最后，整套 Vibe Coding 可以只剩下这一行：

```text
对象 → 态射 → 复合 → 交换图 → 对偶 → 证据
```

对象告诉你，现在有什么。

态射告诉你，发生什么变化。

复合告诉你，变化怎么连起来。

恒等态射提醒你，有些东西经过系统以后不应该改变。

交换图检查不同路径是不是仍然指向同一个要求。

对偶让你在失败的时候，从结果往回找。

证据最后回答那句最麻烦的话：

它真的完成了吗。

范畴论不替你运行代码。

也不替你申请权限。

不会替你部署服务器。

不会替你判断产品该不该上线。

它只做了一件很安静的事。

把关系画出来。

然后你会开始看见一些以前不明显的东西。

哪根箭头没有证据。

哪个对象从来没有被定义。

哪两条路径嘴上说去同一个地方，实际上早就分开了。

哪个 Agent 生成得很好，却从来没有真正执行。

哪个测试全绿，只是因为它验证了错误的目标。

范畴论负责结构。

模型负责生成。

Harness 负责接触现实。

工具负责执行。

测试负责证明。

人负责最后那一下判断。

它们连起来，才是一条完整的路。

完整教程里还有工具配置、Skill、工作流、实践方法和研究资料。

需要的时候，可以去完整仓库的 `develop` 分支。

但是根上的东西，其实没有那么多。

桌子上有一些对象。

对象之间有一些箭头。

箭头一根接一根。

最后你站在终点，看着测试全部通过。

这时候不要急着说完成。

回头看一眼。

看看那条路。

是不是你一开始要走的那一条。

如果是。

再说完成。
