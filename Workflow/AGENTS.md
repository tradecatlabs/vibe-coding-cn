# Workflow Agent 指南

## 目录职责

`Workflow/` 存放项目开发流程类 Markdown，是开发顺序、质量门禁、版本控制和文档同步规则的流程入口。

## 修改规则

- 新增流程文档时，优先使用 Markdown。
- 流程必须写成可执行步骤，避免只写抽象口号。
- 涉及命令、路径、配置、CI、Git 操作时，必须与仓库当前事实一致。
- 不在本目录保存密钥、Token、本地账号、真实私有项目配置或一次性日志。
- 架构、目录、命令或质量门禁发生变化时，同步更新本目录和根 `README.md` / `AGENTS.md`。

## 验证

```bash
make test
```
